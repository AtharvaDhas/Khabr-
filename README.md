# Khabr-
Khabr is a responsive news aggregator web app that fetches live headlines using NewsAPI. Features include category filtering, keyword search, paginated card layout, and a dark/light mode toggle — all built with pure HTML, CSS, and JavaScript
